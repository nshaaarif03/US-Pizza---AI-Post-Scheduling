@extends('layouts.app')
@section('title','Settings')
@section('content')
<div class="page-heading"><div><p class="eyebrow">YOUR WORKSPACE CONNECTIONS</p><h1>Settings<span class="title-dot">.</span></h1><p class="muted mt-2">A clear view of what’s connected and what’s coming next.</p></div></div>
<div class="max-w-4xl space-y-6"><section class="panel"><div class="section-heading border-b border-zinc-100"><h2>AI integration</h2><x-icon name="spark"/></div><div class="form-body"><div class="flex flex-wrap justify-between gap-4 items-center"><div class="flex gap-4 items-center"><span class="accent-icon"><x-icon name="spark"/></span><div><h3>Google Gemini</h3><p class="muted text-sm mt-1">Campaign planning and platform-specific copy</p></div></div><span class="badge {{ $status === 'Connected' ? 'badge-approved' : 'badge-draft' }}">{{ $status }}</span></div><p class="muted text-sm mt-5">@if($status === 'Not Configured')Ask your administrator to add the Gemini API key to the server configuration. You can explore the workflow with sample plans in the meantime.@elseif($status === 'Connected')The last content generation succeeded within the past hour. This is not a live availability check.@elseif($status === 'Error')The last generation attempt failed. Check the connection, key, model access and quota before trying again.@elseAn API key is configured. Generate a content plan to verify the connection.@endif</p><a href="{{ route('campaigns.create') }}" class="btn btn-secondary mt-5">Open Content Generator<x-icon name="arrow"/></a></div></section>
<section class="panel"><div class="section-heading border-b border-zinc-100"><div><h2>Social media connections</h2><p class="muted text-sm mt-1">Future Integration</p></div><span class="badge badge-draft">Coming later</span></div><div class="divide-y divide-zinc-100">@foreach(\App\Models\Post::PLATFORMS as $platform)<div class="connection-row"><x-platform :platform="$platform"/><span class="muted text-sm sm:ml-auto">Not Connected</span><button class="btn btn-secondary" disabled aria-label="Connect {{ $platform }} — future integration">Connect <span class="text-xs">· Future</span></button></div>@endforeach</div><div class="form-footer"><p class="muted text-sm">Automatic publishing is not available in this prototype. Approved posts offer caption copying and links to each platform for manual posting.</p></div></section>
<section class="panel p-6"><h2>Workspace details</h2><dl class="details-list"><div><dt>Application</dt><dd>AI Social Media Planner · Version 1</dd></div><div><dt>Timezone</dt><dd>Asia/Kuala_Lumpur · UTC+8</dd></div><div><dt>Demo content</dt><dd>Fictional and labeled “Sample”</dd></div><div><dt>Brand asset</dt><dd>{{ file_exists(public_path('images/us-pizza-logo.png')) ? 'US Pizza logo installed' : 'Temporary wordmark · supplied logo pending' }}</dd></div></dl></section></div>
@endsection
